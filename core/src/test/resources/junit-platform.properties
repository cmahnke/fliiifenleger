# Enable class-level parallel test execution in core.
# Methods within a class stay sequential: tile generation already parallelizes
# internally, and some tests mutate shared JVM state (see @Isolated markers).
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=same_thread
junit.jupiter.execution.parallel.mode.classes.default=concurrent
