<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>de.christianmahnke.iiif.fliiifenleger</groupId>
        <artifactId>fliiifenleger-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>wasm-runtime</artifactId>
    <name>Fliiifenleger :: wasm-runtime</name>
    <description>
        Shared WebAssembly runtime layer: engine abstraction (Chicory default,
        GraalVM optional), linear-memory helpers, and the module memory
        contract (wasm_alloc/wasm_free).  Used by the jc2pa and ultrahdr
        modules; contains no codec code itself.
    </description>
    <packaging>jar</packaging>

    <properties>
        <chicory.version>1.7.5</chicory.version>
        <!--
            graalvm.version: must match across polyglot + wasm-community.
            Kept for the OPTIONAL GraalWasm engine.
        -->
        <graalvm.version>24.1.0</graalvm.version>
    </properties>

    <dependencies>
        <!--
            Chicory WebAssembly runtime (pure JVM, zero native dependencies)
            plus its WASI preview 1 host functions and annotations.  The
            chicory-wasi artifact carries @HostModule-annotated classes
            without declaring the annotations artifact transitively
            (upstream packaging gap), hence the explicit dependency.
        -->
        <dependency>
            <groupId>com.dylibso.chicory</groupId>
            <artifactId>runtime</artifactId>
            <version>${chicory.version}</version>
        </dependency>
        <dependency>
            <groupId>com.dylibso.chicory</groupId>
            <artifactId>wasi</artifactId>
            <version>${chicory.version}</version>
        </dependency>
        <dependency>
            <groupId>com.dylibso.chicory</groupId>
            <artifactId>annotations</artifactId>
            <version>${chicory.version}</version>
        </dependency>

        <!--
            GraalVM WebAssembly runtime (OPTIONAL).
            Only needed when running on a GraalVM and selecting
            -Dwasm.engine=graalvm.  Declared optional and excluded from the
            shaded JAR; the default Chicory engine has no dependency on it.
        -->
        <dependency>
            <groupId>org.graalvm.polyglot</groupId>
            <artifactId>polyglot</artifactId>
            <version>${graalvm.version}</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.graalvm.polyglot</groupId>
            <artifactId>wasm-community</artifactId>
            <version>${graalvm.version}</version>
            <type>pom</type>
            <optional>true</optional>
        </dependency>

        <!-- Test scope -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.25.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgs>
                        <arg>-Xlint:all,-processing</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!--
                One fresh JVM per test class: WASM modules (both c2pa-rs and
                the ultrahdr codec) keep process-global state that accumulates
                across operations; per-class forking keeps the suites
                deterministic.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <useModulePath>false</useModulePath>
                    <forkCount>1</forkCount>
                    <reuseForks>false</reuseForks>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
